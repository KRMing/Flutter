import 'package:flutter/material.dart';

class colors {

  List<Color> defaultColors = [Colors.blue, Colors.deepPurple, Colors.indigo];
  List<Color> bgColors = [];

  colors() {
    int dfIndex = 0;
    int bgIndex = 0;

    for (var i = 600; i < 900; ) {

    }
  }
}